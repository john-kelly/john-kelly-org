describe('router', () => {
    const router = require('../../src/router.js');

    beforeEach(() => {
        router.register('.', () => {});
    });

    it('should be able to blah', () => {
        // codes.
    });

    describe('when blah', () => {
        // codes.
    });
});
